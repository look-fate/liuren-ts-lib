# =========================================================================
# 依赖 (Dependencies)
# 这些文件可以通过 `yarn install` 重新生成，不应提交到 git
# =========================================================================
/node_modules

# =========================================================================
# 构建产物 (Build Output)
# 这是由 TypeScript 编译器 (tsc) 生成的，源码才是我们关心的
# =========================================================================
/dist
/build
/.tsbuildinfo

# =========================================================================
# 日志文件 (Logs)
# 记录 yarn 或 npm 运行过程中的调试信息和错误
# =========================================================================
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# =========================================================================
# 测试报告和缓存 (Test Reports & Cache)
# 这是由测试框架（如 Jest）生成的覆盖率报告和缓存文件
# =========================================================================
/coverage
.jest-cache

# =========================================================================
# 环境变量 (Environment Variables)
# 这些文件通常包含敏感信息，如 API 密钥，不应提交
# =========================================================================
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# =========================================================================
# 操作系统生成的文件 (OS Generated Files)
# =========================================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# =========================================================================
# 编辑器和 IDE 的配置文件 (Editor & IDE Configs)
# 这些通常是个人开发环境的配置
# =========================================================================
# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains (WebStorm, IntelliJ)
.idea/

# Sublime Text
*.sublime-workspace

# Vim
*.swp
*.swo

.yarn/*
.yarn

# =========================================================================
# pnpm 相关 (pnpm)
# =========================================================================
.pnpm-store/